// swift-tools-version:5.0
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let pkg = Package(name: "mcMediaExtensions")

pkg.dependencies = [
   .package(path: "../mc-foundation-testability")
   //.package(url: "https://bitbucket.org/vgorloff/mcFoundationTestability.git", .upToNextMinor(from: "1.0.2"))
]

pkg.platforms = [
   .macOS(.v10_13), .iOS(.v11), .tvOS(.v11), .watchOS(.v3)
]
pkg.products = [
   .library(name: "mcMediaExtensions", targets: ["mcMediaExtensions"])
]
pkg.targets = [
   .target(name: "mcMediaExtensions", dependencies: ["mcFoundationTestability"], path: "Sources"),
]
