// swift-tools-version:5.0
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let pkg = Package(name: "mcFoundationTestability")

pkg.dependencies = [
   .package(path: "../mc-foundation-extensions")
   //.package(url: "https://bitbucket.org/vgorloff/mcFoundationExtensions.git", .upToNextMinor(from: "1.0.3"))
]

pkg.platforms = [
   .macOS(.v10_13), .iOS(.v11), .tvOS(.v11), .watchOS(.v3)
]
pkg.products = [
   .library(name: "mcFoundationTestability", targets: ["mcFoundationTestability"])
]
pkg.targets = [
   .target(name: "mcFoundationTestability", dependencies: ["mcFoundationExtensions"], path: "Sources"),
]
