// swift-tools-version:5.0
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let pkg = Package(name: "mcFoundationFormatters")

pkg.dependencies = [
   .package(path: "../mc-math")
   //.package(url: "https://bitbucket.org/vgorloff/mcMath.git", .upToNextMinor(from: "1.0.1"))
]

pkg.platforms = [
   .macOS(.v10_13), .iOS(.v11), .tvOS(.v11), .watchOS(.v3)
]
pkg.products = [
   .library(name: "mcFoundationFormatters", targets: ["mcFoundationFormatters"])
]
pkg.targets = [
   .target(name: "mcFoundationFormatters", dependencies: ["mcMath"], path: "Sources"),
]
