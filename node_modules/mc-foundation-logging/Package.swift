// swift-tools-version:5.0
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let pkg = Package(name: "mcFoundationLogging")

pkg.dependencies = [
   .package(path: "../mc-concurrency-locking"),
   .package(path: "../mc-testability")
   //.package(url: "https://bitbucket.org/vgorloff/mcConcurrencyLocking.git", .upToNextMinor(from: "1.0.1")),
   //.package(url: "https://bitbucket.org/vgorloff/mcTestability.git", .upToNextMinor(from: "1.0.3"))
]

pkg.platforms = [
   .macOS(.v10_13), .iOS(.v11), .tvOS(.v11), .watchOS(.v3)
]
pkg.products = [
   .library(name: "mcFoundationLogging", targets: ["mcFoundationLogging"])
]
pkg.targets = [
   .target(name: "mcFoundationLogging", dependencies: ["mcConcurrencyLocking", "mcTestability"], path: "Sources"),
]
