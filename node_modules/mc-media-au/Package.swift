// swift-tools-version:5.0
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let pkg = Package(name: "mcMediaAU")

pkg.dependencies = [
   .package(path: "../mc-foundation-logging"),
   .package(path: "../mc-foundation-observables"),
   .package(path: "../mc-foundation-extensions")
   // .package(url: "https://bitbucket.org/vgorloff/mcFoundationLogging.git", .upToNextMinor(from: "1.0.3")),
   // .package(url: "https://bitbucket.org/vgorloff/mcFoundationObservables.git", .upToNextMinor(from: "1.0.1")),
   // .package(url: "https://bitbucket.org/vgorloff/mcFoundationExtensions.git", .upToNextMinor(from: "1.0.5"))
]

pkg.platforms = [
   .macOS(.v10_13), .iOS(.v11), .tvOS(.v11), .watchOS(.v3)
]
pkg.products = [
   .library(name: "mcMediaAU", targets: ["mcMediaAU"])
]
pkg.targets = [
   .target(name: "mcMediaAU", dependencies: ["mcFoundationLogging", "mcFoundationObservables", "mcFoundationExtensions"], path: "Sources"),
]
